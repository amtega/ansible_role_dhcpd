---
# Role variables

# Packages necesasry for dpchd service

dhcpd_packages:
  - dhcp

# Include dirs paths

dhcpd_base_dir: /etc/dhcp/dhserver.d
dhcpd_includes_file: "{{ dhcpd_base_dir }}/includes.conf"
